% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers__research_enterprise.R
\name{oauth_provider_cognito}
\alias{oauth_provider_cognito}
\title{Create an Amazon Cognito \link{OAuthProvider} (via OIDC discovery)}
\usage{
oauth_provider_cognito(issuer, name = "cognito", token_auth_style = NULL)
}
\arguments{
\item{issuer}{Exact user pool issuer URL from AWS, for example
\code{"https://cognito-idp.eu-west-1.amazonaws.com/eu-west-1_Example"}.}

\item{name}{Optional provider name (default \code{"cognito"}).}

\item{token_auth_style}{Authentication style for token requests: "header"
(client_secret_basic), "body" (client_secret_post), or "public"
(public client; send \code{client_id} only). The alias \code{"none"} is also
accepted for \code{"public"}. If NULL (default), it is inferred conservatively
from discovery: \code{"header"} (client_secret_basic) is preferred, followed by
\code{"body"} (client_secret_post), then \code{"public"} if \code{none} is advertised and
PKCE is enabled. Set \code{token_auth_style = "public"} explicitly for a public
client registration. JWT methods (\code{"client_secret_jwt"},
\code{"private_key_jwt"}) and mTLS methods (\code{"tls_client_auth"},
\code{"self_signed_tls_client_auth"}) must be selected explicitly. See
\code{\link[=oauth_provider]{oauth_provider()}} for the supported methods and their credentials.}
}
\value{
\link{OAuthProvider} object configured for a Cognito user pool.
}
\description{
Configure OIDC login for an Amazon Cognito user pool. Register an app client
with the authorization code grant, configure a user pool login domain, and
pass the app credentials to \code{\link[=oauth_client]{oauth_client()}} with the enabled OIDC scopes.
}
\details{
Use the exact user pool issuer, not the managed-login or custom domain.
Discovery obtains authorization, token, and UserInfo URLs on that login
domain while retaining the pool issuer for ID token validation. Both the
original \code{cognito-idp} and updated \code{issuer-cognito-idp} issuer forms are
supported, as are AWS partition-specific hostnames. No AWS credentials
are needed for discovery.

See \href{https://docs.aws.amazon.com/cognito/latest/developerguide/federation-endpoints.html}{Cognito endpoints}.
}
\examples{
\dontrun{
oauth_provider_cognito(
  "https://cognito-idp.eu-west-1.amazonaws.com/eu-west-1_Example"
)
}
}
