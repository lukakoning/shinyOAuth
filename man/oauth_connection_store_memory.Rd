% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection_store.R
\name{oauth_connection_store_memory}
\alias{oauth_connection_store_memory}
\alias{print.OAuthConnectionStore}
\title{Create a process-local store for retained OAuth connections}
\usage{
oauth_connection_store_memory(
  max_age = 28800,
  refresh_timeout = 60,
  max_entries = 1000L
)

\method{print}{OAuthConnectionStore}(x, ...)
}
\arguments{
\item{max_age}{Maximum record and transaction-deduplication lifetime in seconds.
Each record must also supply an earlier or equal absolute expiry.}

\item{refresh_timeout}{Maximum time in seconds for a claimed refresh. An
abandoned operation becomes uncertain; its old credentials cannot be retried.}

\item{max_entries}{Maximum live record and transaction-reservation count,
including tombstones and recently expired transactions.
A full store rejects creation instead of evicting another connection.}

\item{x}{An \code{OAuthConnectionStore} adapter to print.}

\item{...}{Unused print arguments.}
}
\value{
An \code{OAuthConnectionStore} adapter with the methods described below.
}
\description{
Stores encrypted credential envelopes and coordinates their lifecycle across
Shiny sessions in one R process. Create one store outside \code{server()}. This
adapter does not survive process restarts and rejects use from a copied worker
process. It is a connection-manager building block, not an authentication API.
}
\details{
Methods are trusted server-side operations. The manager must validate the
current owner session and generation before calling them. The opaque owner
identifier scopes records and can survive session rotation; knowledge of an
owner or connection ID does not authenticate a user.
Records contain ciphertext only. Encryption keys stay with the manager, outside
this adapter. Never expose these methods or credential imports as HTTP routes.
\itemize{
\item \verb{$create(owner, id, transaction, target, fingerprint, sealed, expires_at)}
returns a record with a new revision, or \code{NULL} for a duplicate connection/transaction.
\item \verb{$read(owner, id)} returns the owner's record, or \code{NULL} when absent, expired,
or owned by someone else. It includes the sealed envelope for internal use.
\item \verb{$list(owner)} returns metadata lists without ciphertext or operation IDs.
\item \verb{$begin_refresh(owner, id, revision)} claims an active record and returns its
new revision and operation ID. A conflicting claim returns \code{NULL}.
\item \verb{$commit_refresh(owner, id, operation, revision, sealed)} installs credentials
only for the current claim, returning the updated record or \code{NULL}.
\item \verb{$fail_refresh(owner, id, operation, revision, outcome)} releases a claim only
for \code{"not_consumed"}; \code{"possibly_consumed"} and \code{"consumed"} remove the old
envelope and mark the record \code{"uncertain"}. Returns the record or \code{NULL}.
\item \verb{$disconnect(owner, id, revision)} first installs a credential-free tombstone,
then returns \code{list(record, previous)} for bounded remote cleanup. Conflicts
return \code{NULL}; the caller must reload before retrying.
\item \verb{$disconnect_owner(owner)} tombstones all of that owner's records and returns
the previous records for bounded cleanup. Other owners are unaffected.
}

Reads expire abandoned refresh claims before returning data. Tombstones and
transaction IDs remain at least until the original absolute expiry so late completions
cannot restore a disconnected grant. Operations are synchronous and do not yield
or perform network calls. Atomicity applies to this R process only. A separate
shared backend needs its own verified concurrency contract.
}
\seealso{
\code{\link[=oauth_target]{oauth_target()}}, \code{\link[=oauth_connection]{oauth_connection()}}
}
