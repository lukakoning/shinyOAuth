% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers__research_enterprise.R
\name{oauth_provider_authentik}
\alias{oauth_provider_authentik}
\title{Create an authentik \link{OAuthProvider} (via OIDC discovery)}
\usage{
oauth_provider_authentik(
  base_url,
  application_slug,
  name = "authentik",
  token_auth_style = NULL
)
}
\arguments{
\item{base_url}{authentik instance URL, for example \code{"https://auth.example.com"}.}

\item{application_slug}{Application slug configured in authentik (not the
display name or client ID).}

\item{name}{Optional provider name (default \code{"authentik"}).}

\item{token_auth_style}{Authentication style for token requests: "header"
(client_secret_basic), "body" (client_secret_post), or "public"
(public client; send \code{client_id} only). The alias \code{"none"} is also
accepted for \code{"public"}. If NULL (default), it is inferred conservatively
from discovery: \code{"header"} (client_secret_basic) is preferred, followed by
\code{"body"} (client_secret_post), then \code{"public"} if \code{none} is advertised and
PKCE is enabled. Set \code{token_auth_style = "public"} explicitly for a public
client registration. JWT methods (\code{"client_secret_jwt"},
\code{"private_key_jwt"}) and mTLS methods (\code{"tls_client_auth"},
\code{"self_signed_tls_client_auth"}) must be selected explicitly. See
\code{\link[=oauth_provider]{oauth_provider()}} for the supported methods and their credentials.}
}
\value{
\link{OAuthProvider} object configured for the authentik application.
}
\description{
Configure login for an authentik OAuth2/OIDC application. Supply the
instance URL and application slug, then pass the registered credentials
to \code{\link[=oauth_client]{oauth_client()}} with the scopes configured in authentik.
}
\details{
Uses authentik's default per-application issuer mode and preserves its
trailing slash. Global issuer mode is not supported by this preset;
configure the endpoints explicitly with \code{\link[=oauth_provider]{oauth_provider()}} for that mode.
For refresh tokens, both request \code{"offline_access"} and enable the
corresponding scope mapping in authentik. Configure a signing key that
supports RS256 for OIDC discovery.
See \href{https://docs.goauthentik.io/add-secure-apps/providers/oauth2/}{authentik's provider guide}.
}
\examples{
\dontrun{
oauth_provider_authentik("https://auth.example.com", "shiny-app")
}
}
