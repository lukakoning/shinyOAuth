% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smart_discovery.R
\name{smart_discover}
\alias{smart_discover}
\title{Discover SMART on FHIR server metadata}
\usage{
smart_discover(fhir_base, endpoint_hosts = NULL, allow_http_loopback = FALSE)
}
\arguments{
\item{fhir_base}{Trusted FHIR base URL, including its complete path, for
example \code{"https://ehr.example/fhir/R4"}.}

\item{endpoint_hosts}{Character vector of exact permitted hostnames for
recognized metadata URLs. \code{NULL} defaults to the FHIR base hostname.
An explicit vector replaces that default; include every permitted host.
Hostnames are case-insensitive. Wildcards, URLs, and ports are not accepted.}

\item{allow_http_loopback}{Logical, default \code{FALSE}. Explicit development
exception permitting HTTP only at \code{localhost}, \verb{127.0.0.1}, or \verb{::1}.
This applies to the base and metadata URLs and does not change global
options. It does not establish production TLS interoperability.}
}
\value{
A plain named list with \code{fhir_base} (the supplied identifier),
\code{discovery_url} (the requested URL), \code{smart_version} (\code{"2.2.0"}, the
validation baseline, not a detected server version), \code{metadata} (the parsed
document, with JSON arrays as lists), \code{endpoint_hosts} (the normalized
policy), and \code{allow_http_loopback}. No client, token, or live cache is stored
in the result. Invalid input or endpoint policy raises a
\code{shinyOAuth_config_error}; malformed metadata raises a
\code{shinyOAuth_parse_error}; failed HTTP requests raise a
\code{shinyOAuth_http_error}.
}
\description{
Read and validate the SMART App Launch STU 2.2 discovery document for a
configured FHIR server. This returns server metadata for application setup;
it does not register an app, choose client credentials, or start authorization.
}
\details{
Call once during application setup, outside \code{server()}. The request appends
\verb{/.well-known/smart-configuration} to the full FHIR base, removing its terminal
slash first. The supplied base and returned endpoint/issuer strings are
retained exactly, so discovery does not change protocol identifiers.

This is separate from \code{\link[=oauth_provider_oidc_discover]{oauth_provider_oidc_discover()}}. OAuth-only SMART
metadata need not contain OIDC issuer or signing-key information. Advertised
\code{sso-openid-connect} requires both \code{issuer} and \code{jwks_uri}; their presence
does not validate an identity or fetch OIDC metadata or keys.

Required SMART fields, conditional launch/SSO fields, and advertised
asymmetric-authentication metadata are checked. S256 must be advertised and
plain PKCE is rejected. Client-specific capability, scope, key and algorithm
selection belongs to the application's registration configuration.
\code{scopes_supported} is informative, not an exhaustive permission allowlist.
}
\section{Network and trust policy}{

Supply a trusted, deployment-configured base, never an arbitrary browser
query parameter. The base must be an absolute HTTPS URL without userinfo,
query, fragment, or ambiguous path syntax. Discovered URLs must be absolute;
this reader does not repair relative URLs from legacy servers.

\code{endpoint_hosts} applies to the issuer and these top-level URL fields when
present: \code{authorization_endpoint}, \code{token_endpoint}, \code{jwks_uri},
\code{registration_endpoint}, \code{management_endpoint}, \code{introspection_endpoint},
\code{revocation_endpoint}, \code{userinfo_endpoint},
\code{pushed_authorization_request_endpoint}, \code{smart_app_state_endpoint}, and
\code{user_access_brand_bundle}. Matching uses exact hostnames, independently of
port. This is a discovery policy; it does not authorize resource requests.
The generic \code{shinyOAuth.allowed_hosts} option can further restrict these URLs.

The request carries no OAuth credentials. Redirects are refused even when
the generic redirect option is enabled. Existing package TLS, timeout,
response-size, and retry protections apply. JSON must be an object with no
duplicate members; arrays and recognized URL fields have bounded validation.
Errors do not include response bodies or returned metadata values.

Unknown extensions, including \code{associated_endpoints}, are retained as data
only. Their URLs are not fetched or approved for credential use. No automatic
metadata cache is used: every call reads a new snapshot. Applications should
review metadata changes before replacing configuration; do not rediscover
endpoints during a pending authorization or to reinterpret a retained grant.
}

\examples{
\dontrun{
site <- smart_discover(
  "https://ehr.example/fhir/R4",
  endpoint_hosts = c("ehr.example", "login.example")
)
site$metadata$token_endpoint
site$metadata$capabilities

}
}
\references{
\href{https://hl7.org/fhir/smart-app-launch/STU2.2/conformance.html}{SMART STU 2.2 discovery}
and \href{https://hl7.org/fhir/smart-app-launch/STU2.2/client-confidential-asymmetric.html}{asymmetric client metadata}.
}
\seealso{
\code{\link[=oauth_provider_oidc_discover]{oauth_provider_oidc_discover()}}, \code{\link[=oauth_target]{oauth_target()}}
}
