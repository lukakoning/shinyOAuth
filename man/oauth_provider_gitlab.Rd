% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers__research_enterprise.R
\name{oauth_provider_gitlab}
\alias{oauth_provider_gitlab}
\title{Create a GitLab \link{OAuthProvider} (via OIDC discovery)}
\usage{
oauth_provider_gitlab(
  base_url = "https://gitlab.com",
  name = "gitlab",
  token_auth_style = NULL
)
}
\arguments{
\item{base_url}{GitLab instance URL, including HTTPS and any deployment
subpath. Defaults to \code{"https://gitlab.com"}.}

\item{name}{Optional provider name (default \code{"gitlab"}).}

\item{token_auth_style}{Authentication style for token requests: "header"
(client_secret_basic), "body" (client_secret_post), or "public"
(public client; send \code{client_id} only). The alias \code{"none"} is also
accepted for \code{"public"}. If NULL (default), it is inferred conservatively
from discovery: \code{"header"} (client_secret_basic) is preferred, followed by
\code{"body"} (client_secret_post), then \code{"public"} if \code{none} is advertised and
PKCE is enabled. Set \code{token_auth_style = "public"} explicitly for a public
client registration. JWT methods (\code{"client_secret_jwt"},
\code{"private_key_jwt"}) and mTLS methods (\code{"tls_client_auth"},
\code{"self_signed_tls_client_auth"}) must be selected explicitly. See
\code{\link[=oauth_provider]{oauth_provider()}} for the supported methods and their credentials.}
}
\value{
\link{OAuthProvider} object configured for GitLab.
}
\description{
Configure OIDC sign-in with GitLab.com or a self-managed GitLab instance.
Register an OAuth application on that instance, then pass its credentials
to \code{\link[=oauth_client]{oauth_client()}} with \code{scopes = c("openid", "profile", "email")}.
}
\details{
Supply only the scopes your app needs; \code{"openid"} is enough to request an
ID token. API and repository access require separate GitLab scopes.
See \href{https://docs.gitlab.com/integration/openid_connect_provider/}{GitLab's OIDC guide}.
}
\examples{
\dontrun{
oauth_provider_gitlab()
oauth_provider_gitlab("https://gitlab.example.edu")
}
}
