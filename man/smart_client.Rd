% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smart_client.R
\name{smart_client}
\alias{smart_client}
\title{Configure a SMART on FHIR app registration}
\usage{
smart_client(
  discovery,
  client_id,
  redirect_uri,
  scopes,
  required_scopes = scopes,
  token_auth_style = c("public", "header", "private_key_jwt"),
  client_secret = character(),
  client_assertion_private_key = NULL,
  client_assertion_private_key_kid = NULL,
  client_assertion_alg = "RS384",
  launch = c("standalone", "ehr"),
  identity = c("none", "fhirUser"),
  allow_v1 = FALSE,
  response_mode = NULL,
  authorization_server_mode = "single",
  authorization_server_redirect_uris = character(),
  state_store = cachem::cache_mem(max_age = 300),
  state_key = random_urlsafe(128),
  state_payload_max_age = 300,
  label = "FHIR server",
  authorization_method = "GET"
)
}
\arguments{
\item{discovery}{A plain snapshot returned by \code{\link[=smart_discover]{smart_discover()}}. Its metadata
and endpoint policy are revalidated locally; this performs no network calls.}

\item{client_id, redirect_uri}{App registration values. The callback must use
HTTPS, except for the snapshot's explicit HTTP loopback development policy.}

\item{scopes}{Permissions to request, without automatic wildcard/offline
access. Standalone patient scopes require \code{launch/patient}. EHR clients add
\code{launch}. Supported scope syntax must be advertised through \code{permission-v2}
(or the explicit v1 compatibility selection).}

\item{required_scopes}{Minimum permissions, defaulting to \code{scopes}. Pass a
subset to accept reduced grants as limited connections. Identity scopes are
always required when identity is enabled. Unsupported comparisons fail closed.}

\item{token_auth_style}{Registration type: \code{"public"}, \code{"header"} for a
symmetric secret using HTTP Basic, or \code{"private_key_jwt"}. Selection must
agree with advertised capabilities. Confidential methods must also agree
with authentication metadata; public clients do not authenticate and need
no \code{"none"} entry in that metadata.}

\item{client_secret}{Secret for a symmetric registration; otherwise omit.}

\item{client_assertion_private_key, client_assertion_private_key_kid}{Private
signing key and registered key ID for asymmetric authentication; see
\code{\link[=oauth_client]{oauth_client()}}. Required for \code{"private_key_jwt"}.}

\item{client_assertion_alg}{\code{"RS384"} (default) or \code{"ES384"}; the key and server
metadata must support the selected algorithm. Other styles omit assertions.}

\item{launch}{\code{"standalone"} or \code{"ehr"}, matching the registered app flow.}

\item{identity}{\code{"none"} (default) or \code{"fhirUser"}. A validated \code{fhirUser}
claim may be an absolute URL or a supported resource instance reference
relative to this client's FHIR base, such as \code{"Practitioner/example"}.}

\item{allow_v1}{Explicit compatibility flag enabling \code{.read}, \code{.write} and
\verb{.*}; requires the advertised \code{permission-v1} capability. Default \code{FALSE}.}

\item{response_mode}{\code{NULL}, \code{"query"}, or \code{"form_post"}. Advertised response
modes, when present, must allow the selection.}

\item{authorization_server_mode, authorization_server_redirect_uris}{See
\code{\link[=oauth_client]{oauth_client()}}. Multiple clients use distinct registered callback routes.}

\item{state_store, state_key, state_payload_max_age}{See \code{\link[=oauth_client]{oauth_client()}}.}

\item{label}{Display label, default \code{"FHIR server"}; no credentials or context.}

\item{authorization_method}{\code{"GET"} (default) or \code{"POST"} for the outgoing
browser request. POST requires the discovered \code{authorize-post} capability
and uses the module's \code{request_login()} or \code{\link[=prepare_authorization_request]{prepare_authorization_request()}}.}
}
\value{
An \link{OAuthClient}, usable by the existing module or the separate
connection manager. \verb{@resource_bases} contains the approved \code{fhir} base;
\verb{@required_scopes} and \verb{@label} use the ordinary client properties. \verb{@smart}
describes the selected profile. The client contains registration settings,
never a user's token or launch context. Configure it outside \code{server()}.
}
\description{
Combine a reviewed \code{\link[=smart_discover]{smart_discover()}} snapshot with an existing app
registration. The result is an \link{OAuthClient} for \code{\link[=oauth_connections]{oauth_connections()}}, with
the resource ID \code{"fhir"}. Discovery does not register the app or grant access.
}
\details{
This constructor selects SMART 2.2 scope rules, S256 PKCE and the exact FHIR
base as the authorization request's \code{aud}. Identity is opt-in: \code{"fhirUser"}
requests and requires \verb{openid fhirUser}, signed ID-token validation and nonce
binding. \code{"none"} does not enable OIDC because an issuer happens to be present.
A patient in context is independent of the authenticated user and local owner.

Only direct authorization requests and query/form POST callbacks are currently
supported. JAR, PAR, JARM, DPoP, mTLS and remote freshness requirements need
separate SMART composition work; this constructor provides no overrides for
those features. EHR clients require a fresh registered launch transaction.
Configure standalone and EHR registrations as separate clients when both are
needed. No launch handle is stored in shared provider configuration.
Local usability policy requires a positive \code{expires_in} in initial and
refresh responses; the generic assumed lifetime is not used for SMART.
}
\examples{
\dontrun{
site <- smart_discover("https://ehr.example/fhir/R4")
client <- smart_client(site, "registered-app", "https://app.example/callback",
  scopes = c("launch/patient", "patient/Patient.r"),
  required_scopes = "patient/Patient.r", token_auth_style = "public")
client@smart$launch
}
}
\references{
\href{https://hl7.org/fhir/smart-app-launch/STU2.2/app-launch.html}{SMART 2.2 launch}
and \href{https://hl7.org/fhir/smart-app-launch/STU2.2/client-confidential-asymmetric.html}{client authentication}.
}
