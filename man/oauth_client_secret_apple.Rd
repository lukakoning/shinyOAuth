% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers__apple.R
\name{oauth_client_secret_apple}
\alias{oauth_client_secret_apple}
\title{Create a client secret for Sign in with Apple}
\usage{
oauth_client_secret_apple(
  client_id,
  team_id,
  key_id,
  private_key,
  expires_in = 15776700,
  issued_at = Sys.time(),
  audience = "https://appleid.apple.com"
)
}
\arguments{
\item{client_id}{Apple Services ID or App ID used as the OAuth client id}

\item{team_id}{Apple Developer Team ID. Apple documents this as a
10-character identifier}

\item{key_id}{Apple Sign in with Apple private-key identifier (\code{kid}). Apple
documents this as a 10-character identifier}

\item{private_key}{Apple private key as an \code{openssl::key} or PEM string. The
key must be compatible with \code{ES256} (P-256 ECDSA)}

\item{expires_in}{Positive lifetime in seconds. Must be no more than
\code{15777000} seconds (six months). Defaults to \code{15776700} seconds, leaving a
five-minute margin below Apple's documented maximum}

\item{issued_at}{Issue time for the JWT. Defaults to \code{Sys.time()}}

\item{audience}{Audience claim. Defaults to \code{"https://appleid.apple.com"}}
}
\value{
A compact signed JWT string suitable for \code{oauth_client(..., client_secret = ...)}
}
\description{
Use your Apple developer key to create the client secret expected by Sign in
with Apple. Pass the returned string as \code{client_secret} to \code{\link[=oauth_client]{oauth_client()}}.
Unlike a fixed password, this secret expires; replace it before its expiry.
}
\details{
The helper signs a JWT with ES256. It places your Team ID in \code{iss}, your
client ID in \code{sub}, Apple's URL in \code{aud}, and the key ID in the header.
The lifetime must not exceed 15,777,000 seconds (about six months).
}
\examples{
\dontrun{
key <- openssl::ec_keygen(curve = "P-256")

oauth_client_secret_apple(
  client_id = "com.example.web",
  team_id = "ABCDEFGHIJ",
  key_id = "ABC123DEFG",
  private_key = key
)
}

}
