% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes__OAuthConnectionRef.R
\name{OAuthConnectionRef}
\alias{OAuthConnectionRef}
\title{OAuthConnectionRef R6 class}
\description{
Make API requests using a Shiny session's current OAuth credentials and the
client/API configuration supplied by an \link{OAuthTarget}. For example, a hospital
connection selects that hospital's API address and reads the session's current
token for each request. Create it once inside \code{server()} with
\code{\link[=oauth_connection]{oauth_connection()}}. Use \verb{$request()} to call an approved API, \verb{$is_usable()}
to check local availability and \verb{$summary()} for status without credentials.
}
\details{
"Ref" means a reference to credentials that can change, rather than a copy of
a token. The existing reactive token already updates on refresh; this object
combines that lookup with client selection, API-address restrictions and
session checks. The application must supply the matching module's token source.
These are optional shinyOAuth conveniences, not SMART on FHIR protocol objects.

Each operation resolves the current reactive token, so refresh and logout
are reflected without replacing the reference. Token lifecycle remains owned
by \code{\link[=oauth_module_server]{oauth_module_server()}}. The reference expires when its owning session
closes and does not retain credentials across redirects.

Call \verb{$is_usable()}, \verb{$summary()} and \verb{$request()} in the owning session's
reactive context. If the connection cannot be resolved, \verb{$is_usable()} returns
\code{FALSE}; \verb{$summary()} and \verb{$request()} raise an error. The ID is read-only and
cloning is disabled. The class generator is internal; \code{\link[=oauth_connection]{oauth_connection()}}
establishes the session binding required by applications.
}
\examples{
\dontrun{
# Configure an OAuthClient outside server(), then bind its approved API:
target <- oauth_target(
  client,
  c(api = "https://api.example/v1"),
  required_scopes = "read"
)
server <- function(input, output, session) {
  auth <- oauth_module_server("auth", target$client)
  connection <- oauth_connection(target, shiny::reactive(auth$token))
  output$status <- shiny::renderText(connection$summary()$status)
  records <- shiny::reactive({
    shiny::req(connection$is_usable())
    response <- connection$request("api", "records", required_scopes = "read")
    httr2::resp_body_json(response)
  })
}
}
}
\seealso{
\code{\link[=oauth_connection]{oauth_connection()}}, \link{OAuthTarget}, \code{\link[=perform_resource_req]{perform_resource_req()}}
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{id}}{Read-only opaque character string identifying this reference.
It is generated by \code{\link[=oauth_connection]{oauth_connection()}} and is not an access token or a
persistent connection identifier.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-OAuthConnectionRef-initialize}{\code{OAuthConnectionRef$new()}}
    \item \href{#method-OAuthConnectionRef-is_usable}{\code{OAuthConnectionRef$is_usable()}}
    \item \href{#method-OAuthConnectionRef-summary}{\code{OAuthConnectionRef$summary()}}
    \item \href{#method-OAuthConnectionRef-request}{\code{OAuthConnectionRef$request()}}
    \item \href{#method-OAuthConnectionRef-print}{\code{OAuthConnectionRef$print()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-OAuthConnectionRef-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-OAuthConnectionRef-initialize}{}}}
\subsection{\code{OAuthConnectionRef$new()}}{
  Initialize a reference. This constructor is for internal use;
applications should use \code{\link[=oauth_connection]{oauth_connection()}} to establish session ownership.
Calling it again on an initialized reference is an error.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{OAuthConnectionRef$new(id, target, resolve)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{id}}{Opaque character string identifying the reference.}
      \item{\code{target}}{The \link{OAuthTarget} to bind to this reference.}
      \item{\code{resolve}}{Internal function with no arguments that enforces session
ownership and returns a list with \code{target} identical to this reference's
target and \code{token} containing the current \link{OAuthToken} or \code{NULL}.
It must raise an error when the owning session is unavailable.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new \code{OAuthConnectionRef} instance.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-OAuthConnectionRef-is_usable"></a>}}
\if{latex}{\out{\hypertarget{method-OAuthConnectionRef-is_usable}{}}}
\subsection{\code{OAuthConnectionRef$is_usable()}}{
  Check whether the current token is locally usable. This checks token
presence, known unexpired lifetime and the target's required scopes.
It does not refresh the token, contact the provider or guarantee remote
authorization. Request-specific scopes are checked by \verb{$request()}.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{OAuthConnectionRef$is_usable()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A single logical value: \code{TRUE} for an \code{active} or \code{limited}
connection, otherwise \code{FALSE}, including when resolution fails.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-OAuthConnectionRef-summary"></a>}}
\if{latex}{\out{\hypertarget{method-OAuthConnectionRef-summary}{}}}
\subsection{\code{OAuthConnectionRef$summary()}}{
  Resolve the current connection and return status information without
credentials, identity claims or token extension fields. Raises an error
when called outside the owning session or after that session closes.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{OAuthConnectionRef$summary()}
    \if{html}{\out{</div>}}
  }
  \subsection{Details}{
    Status is evaluated in this order:
\itemize{
\item \code{disconnected}: there is no current token.
\item \code{expiry_unknown}: the token's expiry is unknown.
\item \code{expired}: the token has reached its expiry time.
\item \code{insufficient_scope}: the grant lacks a target-required scope.
\item \code{limited}: required scopes are covered, but some other requested scopes
are absent from the grant.
\item \code{active}: all requested scopes are covered.
}

Scope checks use the token's current \code{granted_scopes}, which may be
assumed or carried forward when a provider omits scope information.
See \link{OAuthToken} for the distinction from verified scope evidence.
  }
  \subsection{Returns}{
    A named list with the following entries:
\itemize{
\item \code{connection_id}: the reference's character ID.
\item \code{target_label}: the target's application-defined character label.
\item \code{status}: one of the character values listed in this method's details.
\item \code{expires_at}: numeric seconds since the Unix epoch, \code{NA_real_} when
there is no token or its expiry is unknown, or \code{Inf} for a
non-expiring token.
\item \code{resource_ids}: character vector of the target's approved resource IDs.
}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-OAuthConnectionRef-request"></a>}}
\if{latex}{\out{\hypertarget{method-OAuthConnectionRef-request}{}}}
\subsection{\code{OAuthConnectionRef$request()}}{
  Resolve the current token and perform an authenticated request within a
named resource base. The connection must be usable, and its current grant
must cover any scopes required for this operation.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{OAuthConnectionRef$request(
  resource_id,
  path = "",
  query = NULL,
  method = "GET",
  required_scopes = character()
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{resource_id}}{Single character string naming an entry in the target's
\code{resource_bases}.}
      \item{\code{path}}{Single character string resolved relative to the selected base
directory; \code{""} selects the base itself. Absolute and root-relative URLs,
including pagination links, must remain within the same approved origin
and base path. Dot segments and ambiguous encodings are rejected.}
      \item{\code{query}}{Optional named list of query parameters, or \code{NULL}.}
      \item{\code{method}}{Single HTTP method string, defaulting to \code{"GET"}. \code{TRACE}
and \code{TRACK} are rejected by the resource transport.}
      \item{\code{required_scopes}}{Character vector of scopes required for this
operation, in addition to the target's required scopes. They must have
been requested by the client and be covered by the current grant.
\code{character()} adds no operation-specific scope check.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Details}{
    Uses \code{\link[=perform_resource_req]{perform_resource_req()}} with the target's client for Bearer, DPoP
and mTLS authentication. Redirects are never followed. Transport error
messages are redacted to exclude resource paths, queries and response
bodies. Scope requirements are supplied by the application; they cannot
be inferred from an arbitrary API's HTTP method and path.
  }
  \subsection{Returns}{
    An \link[httr2:httr2]{httr2} response object. Invalid resources, unusable connections,
insufficient scopes and transport failures raise errors.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-OAuthConnectionRef-print"></a>}}
\if{latex}{\out{\hypertarget{method-OAuthConnectionRef-print}{}}}
\subsection{\code{OAuthConnectionRef$print()}}{
  Print the class name and session-binding description, with credentials
redacted. This does not resolve the current token.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{OAuthConnectionRef$print(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Unused; accepted for compatibility with \code{\link[base:print]{base::print()}}.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    This reference, invisibly.
  }
}

}
