% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers__research_enterprise.R
\name{oauth_provider_surfconext}
\alias{oauth_provider_surfconext}
\title{Create a SURFconext \link{OAuthProvider} (via OIDC discovery)}
\usage{
oauth_provider_surfconext(
  environment = c("production", "test"),
  name = "surfconext",
  token_auth_style = NULL
)
}
\arguments{
\item{environment}{SURFconext environment: \code{"production"} (default) or \code{"test"}.}

\item{name}{Optional provider name (default \code{"surfconext"}).}

\item{token_auth_style}{Authentication style for token requests: "header"
(client_secret_basic), "body" (client_secret_post), or "public"
(public client; send \code{client_id} only). The alias \code{"none"} is also
accepted for \code{"public"}. If NULL (default), it is inferred conservatively
from discovery: \code{"header"} (client_secret_basic) is preferred, followed by
\code{"body"} (client_secret_post), then \code{"public"} if \code{none} is advertised and
PKCE is enabled. Set \code{token_auth_style = "public"} explicitly for a public
client registration. JWT methods (\code{"client_secret_jwt"},
\code{"private_key_jwt"}) and mTLS methods (\code{"tls_client_auth"},
\code{"self_signed_tls_client_auth"}) must be selected explicitly. See
\code{\link[=oauth_provider]{oauth_provider()}} for the supported methods and their credentials.}
}
\value{
\link{OAuthProvider} object configured for SURFconext.
}
\description{
Configure institutional OIDC login through SURFconext. Register your service
with SURFconext before using its client credentials with \code{\link[=oauth_client]{oauth_client()}}.
}
\details{
Request the \code{"openid"} scope. Additional claims depend on your service's
agreed attribute release policy; requesting \code{"email"} or \code{"profile"} does
not guarantee those attributes will be released. Use the validated issuer
and \code{sub} to identify a user, rather than assuming email is present.
Test and production environments require their own service configuration.
See \href{https://servicedesk.surf.nl/wiki/spaces/IAM/pages/128909810/SURFconext+for+Service+Providers}{SURFconext for service providers}.
}
\examples{
\dontrun{
oauth_provider_surfconext()
oauth_provider_surfconext(environment = "test")
}
}
